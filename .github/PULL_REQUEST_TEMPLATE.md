<!-- Keep the PR scoped to one concern. -->

## What this changes

## Why

<!-- For content changes: link the primary source for any factual,
     regulatory, or vendor claim (MITRE page, CFR section, vendor doc). -->

## Checklist

- [ ] `pnpm build` passes locally (includes the glossary and tooltip checks)
- [ ] `pnpm lint` passes
- [ ] If a glossary page changed: `pnpm glossary` was run and the regenerated `src/data/glossary.ts` is included
