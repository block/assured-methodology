---
title: "Detection mechanisms"
description: "The four families of detection logic that produce alerts: signature-based, anomaly-based, rule-based, and behavioral analytics."
---

import Callout from "@/components/assured/Callout.astro";
import KeyTakeaway from "@/components/assured/KeyTakeaway.astro";
import ContinueCard from "@/components/assured/ContinueCard.astro";
import SubPageHero from "@/components/assured/SubPageHero.astro";
import PillNav from "@/components/assured/PillNav.astro";
import DefineTerm from "@/components/assured/DefineTerm.astro";

<SubPageHero
  phase="alert"
  parentLetter="A"
  pillarIndex="01"
  pillarTotal="03"
  chapterHref="/docs/alert/"
  chapterLabel="Alert"
  lede="Every alert has a provenance: the detection logic that produced it. Two alerts with the same name from different engines are not the same alert. The mechanism tells the analyst how much to trust the signal, what the engine is likely to have missed, and what evidence to gather next."
/>

<PillNav
  chapter="alert"
  label="Alert chapter"
  items={[
    { label: "Detection mechanisms", href: "/docs/alert/detection-mechanisms/", phase: "alert" },
    { label: "Validation",           href: "/docs/alert/validation/",            phase: "alert" },
    { label: "Parsing",              href: "/docs/alert/parsing/",               phase: "alert" },
  ]}
/>

Modern security platforms layer many kinds of <DefineTerm term="Detection Logic" display="detection logic" /> on top of each other. ASSURED groups them into four families. The grouping is this methodology's teaching taxonomy, not an industry standard (NIST SP 800-94, for example, cuts the space differently), but it maps cleanly onto how alerts arrive in practice. Each family is good at something specific and blind to something else. Used together they cover far more ground than any one of them could on its own. When an alert arrives, the first question an analyst asks is which mechanisms produced it — plural on purpose. Real detections are often hybrids: a deterministic Sigma rule that encodes a behavioral parent-child sequence is both rule-based and behavioral, and a "signature" hit may be one condition inside a larger correlation. The families are lenses for reading a detection, not boxes it must fit into, and naming the family is the start of reading the rule that actually fired, never a substitute for it.

<Callout variant="info" title="Why provenance matters">
A high-confidence signature hit on a known <DefineTerm term="Command and Control (C2)" display="C2" /> binary is one alert. A behavioral anomaly suggesting unusual <DefineTerm term="PowerShell" /> child processes is a different alert, even when the underlying activity is the same. The first deserves an immediate response. The second deserves correlation and context first. Detection provenance is what tells the analyst which kind of alert they have in front of them.
</Callout>

## The four families at a glance

<div class="mechs-grid">
  <a href="/docs/alert/detection-mechanisms/signature/" class="mech-card mech-card--signature">
    <div class="mech-card__num">01</div>
    <h3>🔍 Signature-based</h3>
    <p>Pattern matching against known indicators. Fast, precise, useless against anything novel.</p>
    <span class="mech-card__cta">Read →</span>
  </a>
  <a href="/docs/alert/detection-mechanisms/anomaly/" class="mech-card mech-card--anomaly">
    <div class="mech-card__num">02</div>
    <h3>📊 Anomaly-based</h3>
    <p>Statistical deviation from a behavioral baseline. Catches the unknown. Noisy during change.</p>
    <span class="mech-card__cta">Read →</span>
  </a>
  <a href="/docs/alert/detection-mechanisms/rule/" class="mech-card mech-card--rule">
    <div class="mech-card__num">03</div>
    <h3>⚖️ Rule-based</h3>
    <p>Expert-defined conditional logic combining multiple indicators. Tunable, auditable, demanding to maintain.</p>
    <span class="mech-card__cta">Read →</span>
  </a>
  <a href="/docs/alert/detection-mechanisms/behavioral/" class="mech-card mech-card--behavioral">
    <div class="mech-card__num">04</div>
    <h3>🧠 Behavioral analytics</h3>
    <p>Multi-event sequence analysis across users, hosts, and time. Built for stealth attacks.</p>
    <span class="mech-card__cta">Read →</span>
  </a>
</div>

<Callout variant="info" title="The grid behind the four families">
The families are the corners of a 2×2. One axis is how the logic is produced: signature and rule conditions are written by humans; anomaly and behavioral models are learned from data. The other axis is what the logic evaluates: signature and anomaly judge a single event; rule and behavioral evaluate combinations and sequences. That grid is why Snort content matching sits under signature while a Sigma correlation rule sits under rule, even though both are "rules" in everyday speech. And real detections regularly occupy more than one corner: a hand-written rule whose conditions describe a behavior sequence (a LOLBin parent-child chain, say) is rule-based *and* behavioral. When a detection fits two corners, name both — the point of the label is to tell the next reader what kind of logic to inspect, not to win a classification argument.
</Callout>

---

## Trust calibration

Each family carries a different *prior* — a starting posture, useful for the first thirty seconds and no longer. The mechanism label alone never determines confidence: that comes from the actual rule logic, the telemetry behind it, and the rule's measured performance in your environment. Before translating any label into trust, check the local calibration facts: the rule's version and last review, its precision here (what fraction of its recent fires were real), what it is known to miss, how common the matched artifact is in this environment, and whether suppressions or exceptions are currently active on it.

<div class="trust-list">
  <div class="trust-item trust-item--signature">
    <div class="trust-item__label">Signature</div>
    <div class="trust-item__body">High-precision *matching*, narrow scope — but a deterministic match is not a deterministic verdict. A hit on freshly validated C2 infrastructure deserves immediate attention; the same mechanism firing on a stale indicator, a dual-use tool's hash, or a generic heuristic AV name is precise about the artifact and silent about intent. Check the indicator's age, source, and local prevalence before converting the match into confidence. Either way, the family only sees what it has been told to look for.</div>
  </div>
  <div class="trust-item trust-item--anomaly">
    <div class="trust-item__label">Anomaly</div>
    <div class="trust-item__body">Low default confidence, wide scope. The signal is genuine but the meaning is ambiguous. Anomaly alerts almost always need correlation with another data source before action.</div>
  </div>
  <div class="trust-item trust-item--rule">
    <div class="trust-item__label">Rule</div>
    <div class="trust-item__body">Confidence depends on how the rule was written, how recently it was reviewed, and how well it matches current adversary technique. A well-tuned rule is reliable. A stale rule is noise.</div>
  </div>
  <div class="trust-item trust-item--behavioral">
    <div class="trust-item__label">Behavioral</div>
    <div class="trust-item__body">Medium to high confidence when the sequence is clear, but the analyst needs to look at the full timeline that produced the alert, not just the alert itself.</div>
  </div>
</div>

<KeyTakeaway>
Mastery of detection mechanisms is not memorizing each family. It is the habit of asking which mechanisms produced the alert — accepting that the honest answer is often two — then reading the rule that fired and its local track record before deciding how much to trust it. The four deep-dive pages below are reference material, not required reading. Skim the family that matches whatever fires most in your environment, then return to the others when an unfamiliar family shows up.
</KeyTakeaway>

<ContinueCard
  current="alert"
  nextHref="/docs/alert/detection-mechanisms/signature/"
  nextLabel="Signature-based (reference)"
  nextDescription="The oldest and most precise of the four families: deterministic pattern matching against curated indicators of compromise. Read on-demand when a signature alert fires, or skim now if signatures dominate your alert volume."
  ctaLabel="Browse signature-based"
/>

<style>
{`
.mechs-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem;
  margin: 1.5rem 0;
}

@media (min-width: 768px)  { .mechs-grid { grid-template-columns: 1fr 1fr; } }

.mech-card {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  padding: 1.5rem 1.75rem;
  border-radius: 0.85rem;
  background: rgba(15, 23, 42, 0.55);
  border: 1px solid rgba(148, 163, 184, 0.22);
  border-left: 4px solid var(--accent, var(--assured-accent));
  backdrop-filter: blur(8px);
  color: var(--assured-text);
  text-decoration: none;
  transition: transform 0.2s ease, border-left-width 0.15s ease, box-shadow 0.2s ease;
}

.mech-card:hover {
  transform: translateY(-3px);
  border-left-width: 6px;
  box-shadow: 0 18px 36px -16px rgba(0, 0, 0, 0.5);
}

.mech-card--signature  { --accent: #3b82f6; }
.mech-card--anomaly    { --accent: #22c55e; }
.mech-card--rule       { --accent: #a855f7; }
.mech-card--behavioral { --accent: #f97316; }

.mech-card__num {
  position: absolute;
  top: 1rem;
  right: 1.3rem;
  font-size: 0.75rem;
  font-weight: 800;
  color: var(--accent);
  opacity: 0.7;
  letter-spacing: 0.05em;
}

.mech-card h3 {
  margin: 0;
  color: var(--accent);
  font-size: 1.15rem;
}

.mech-card p {
  margin: 0;
  color: var(--assured-text-muted);
  line-height: 1.55;
  font-size: 0.95rem;
}

.mech-card__cta {
  margin-top: auto;
  color: var(--accent);
  font-weight: 600;
  font-size: 0.9rem;
  padding-top: 0.4rem;
}

.trust-list {
  display: flex;
  flex-direction: column;
  gap: 0.6rem;
  margin: 1.25rem 0;
}

.trust-item {
  display: grid;
  grid-template-columns: 9rem 1fr;
  gap: 1rem;
  padding: 1rem 1.4rem;
  background: rgba(15, 23, 42, 0.5);
  border: 1px solid rgba(148, 163, 184, 0.22);
  border-left: 3px solid var(--accent, var(--assured-accent));
  border-radius: 0.7rem;
}

@media (max-width: 640px) {
  .trust-item { grid-template-columns: 1fr; }
}

.trust-item--signature  { --accent: #3b82f6; }
.trust-item--anomaly    { --accent: #22c55e; }
.trust-item--rule       { --accent: #a855f7; }
.trust-item--behavioral { --accent: #f97316; }

.trust-item__label {
  color: var(--accent);
  font-weight: 700;
  font-size: 0.9rem;
  letter-spacing: 0.02em;
}

.trust-item__body {
  color: var(--assured-text-muted);
  line-height: 1.55;
  font-size: 0.93rem;
}
`}
</style>
